import { createApp } from "vue"
import App from "./App.vue"
import router from "@/router/index"
import { registerIcon } from "@/plugins"

import("@/styles/reset.scss")

const app = createApp(App)

// 注册element-plus的icon
registerIcon(app)

app.use(router).mount("#app")
